# Rash Plan

## Restart checkpoint: prepared 2026-08-05 EDT

- [x] Confirm Peter's morning decisions on branch publication and the first safety slice, then execute the approved path. (2026-08-11 15:00 EDT; merge/push authorized; LuaJIT hooks selected.) Curiosity poke: preserve the verified branch work while keeping any privileged prototype explicitly scoped.
- [x] Read `HANDOFF-20260805214713EDT.md`, `AGENTS.md`, the processed Codex verdict, and the current safety/design documents; verify the branch topology before resuming. (2026-08-05 23:03 EDT) Curiosity poke: a green branch still needs a deliberate publication decision and an independent acceptance record.
- [x] **Peter decision:** authorize merging and pushing `sigchld-inprogress` (`63d1f5ff`) and `bash-identity` (`9530eb54`) into published `master`. (2026-08-11 15:00 EDT) Both worktrees are clean and their source changes do not overlap; this planning checkpoint makes integration a normal merge rather than two fast-forwards. Curiosity poke: both branches add `DIVERGENCE.md` entry 5, so the merge must retain the SIGCHLD entries as 3–5 and renumber the identity entry to 6.
- [x] Merge the verified SIGCHLD series first, merge `bash-identity` second, resolve the one ledger conflict, then run `./test` and `./test --zig` before committing and pushing. (2026-08-11 15:13 EDT; merge commits `c00147e8` and `0d27c170`; `origin/master` independently matched `0d27c170`; Mechatron passed that SHA in 54 s. A later local sample exposed the documented residual fourth SIGCHLD loss, so neither result proves it absent.) Treat the generated upstream patch as an artifact: source/test diffs must pass `git diff --check` independently of whitespace preserved in the patch text. Curiosity poke: no test report may be inferred from a pipeline's final status.
- [ ] **Peter action:** send the SIGCHLD patch series from a pristine GNU Bash clone to `bug-bash@gnu.org`, without AI attribution. Attach `BUGFIX_REPRO_REPORTS/sigchld-fixes.patch`; use the three repair reports and the separate async-signal-unsafe-allocation report as the evidence. Confirm the FSF trivial-patch threshold before mailing. Curiosity poke: the fourth defect is reported, not patched; do not imply otherwise.
- [ ] Curate session artifacts after the branch decision: retain and commit the durable `THREAT_MODEL.md` and `GROK_REVIEW_AND_MITIGATIONS.md` if Peter agrees; leave the intentional `AGENTS.md`/`CLAUDE.md` symlinks untracked; then move the read handoff and processed inbox note to Trash and remove the stale `rash-codex` worktree only after its two inbox notes are read. Curiosity poke: do not discard the only copy of a decision or an independent verifier's evidence.
- [x] **Peter decision:** select the first safety implementation slice. (2026-08-11 15:00 EDT; LuaJIT hooks first.) COW-first establishes the truthful rollback boundary for all descendant writes, but needs root-owned Linux setup and does not inspect command shape. Hook-first delivers the agent-mistake guard at the executor AST, but cannot claim arbitrary-write rollback without the COW envelope. Curiosity poke: neither layer substitutes for the other.
- [x] Integrate LuaJIT and ship one useful, advisory-only post-parse lifecycle hook. It recognizes the exact `sudo -S tee` pipeline stage with an input redirection, warns without exposing the pipe input, then runs the original command. (2026-08-11 16:27 EDT; shipped `hooks/warn_sudo_tee.lua`, installed under `$prefix/share/rash/hooks`; GCC and Zig full Nix checks passed.) Curiosity poke: match tree structure and unexpanded words, never a printable-command regex or secret values.
- [x] Load root-owned hooks normally; permit an unowned hook only when `RASH_ALLOW_UNOWNED_HOOKS=1` is explicitly set, with a loud standard-error warning that its policy is advisory and mutable. (2026-08-11 16:27 EDT; the unowned gate, warning, `run()` fallthrough, and late interactive configuration are covered by `tests/hooks.tests`.) Curiosity poke: this variable chooses a lower-trust hook source; it must never suppress any loaded hook or become a generic bypass token.
- [x] Add `RASH_HOOK_RELOAD=mtime`, an explicit development mode that compares the hook-directory manifest only at an outermost executor entry and reloads if its `.lua` files were added, removed, renamed, edited, or had admission-relevant metadata changed. (2026-08-15 17:47 EDT; cached directory and per-file metadata detects source/admission changes, while failed candidates retain the working hook state. GCC and Zig full Nix checks passed.) Curiosity poke: a directory mtime alone misses an in-place edit, whereas per-node checks must not run for nested AST nodes.
- [x] Add the imperative `reloadhooks` builtin, exposed only when `RASH_HOOK_RELOAD_BUILTIN=1`; otherwise retain Bash's ordinary command-not-found behavior. (2026-08-15 17:47 EDT; flag-gated registration stays absent from lookup/help/completion, and candidate replacement retains a working observer after a rejected reload. GCC and Zig full Nix checks passed.) Curiosity poke: the exposed builtin needs atomic replace-or-retain semantics so a syntax error or rejected replacement does not leave a live session with no observer.
- [x] Start the smallest LuaJIT hook-loading boundary with a failing permission/bypass test and an explicit acceptance oracle. (2026-08-11 16:27 EDT; red tests covered absent hooks, unowned rejection, an uninstalled packaged hook, and late configuration; green GCC and Zig Nix checks cover the complete upstream suite.) Curiosity poke: hooks must never accept an inheritable bypass value, and the COW fixture must prove both the protected boundary and its limit.
- [x] **Safety upgrade A:** enforcing `rash.deny` + `rash.spawn()` + packaged `deny_sudo_tee` / `deny_sensitive_clobber`. (2026-08-27 ~23:10 EDT; `tests/deny-hooks.*` green with hooks/sudo-tee/identity.) Root-owned hooks fail closed; unowned `rash.deny` is advisory-only; safe Lua `base`+`string` only. Curiosity poke: hooks see the outermost command of an `execute_command` entry — a trailing `; echo` hides a pipeline from policy unless the pipeline is its own `-c` unit.
- [x] **Expanded before/after hooks:** `rash.before` after `expand_words` (aliases already gone at parse; vars/globs expanded); `rash.after` with status + capped stdout/stderr when not in a pipeline. (2026-09-04 ~20:10 EDT; `tests/before-after-hooks.*` + full `./test` green.) Curiosity poke: this stage can see secrets — keep packaged policy at parse stage where possible.
- [ ] **Safety upgrade B (follow-on):** catalog clobber undo at `redir.c` (structured preimages).
- [ ] **Safety upgrade C (follow-on):** content-hash exec allowlist at `shell_execve`.
- [ ] **Safety upgrade D (follow-on):** OverlayFS COW workspace launcher.
- [ ] Extend `--emit-ast` v2 only after the selected safety slice has a passing end-to-end gate: recurse through `for`, `if`, `group`, `subshell`, `case`, `function_def`, and `coproc`; emit each type's recorded line; map `W_*` bits to names. Curiosity poke: unsupported structures must never be indistinguishable from absent structures.

- [x] Mirror the canonical GNU Bash repository to `pmarreck/rash`, retaining Savannah as the fetch-only `upstream` remote. (2026-07-21 11:15 EDT)
- [x] Establish and record a hermetic upstream build baseline before any broad rename. Curiosity poke: preserve the upstream `master` branch convention and GPL provenance. (2026-07-22 21:32 EDT; pinned Nix flake and `./build` pass. The review later proved the original test runner false-green, so complete-suite portability is tracked separately.)
- [~] Define Rash's purpose and document the initial safety architecture without changing shell semantics accidentally. **Name settled 2026-08-27:** Rash = **Reversible Auditable/Agent-safe Shell, a fork of Bash** (`--about` + README + `DIVERGENCE.md`). Architecture docs still open (`HOOKS_DESIGN.md` / `THREAT_MODEL.md` / COW). Curiosity poke: the slash in Auditable/Agent-safe is deliberate — one A, two jobs; the Bash clause keeps provenance on the about line.
- [ ] Rename the live project surface from Bash to Rash carefully, with a tested reversible mapping and an explicit decision about historical/GPL attribution.
- [ ] Assess a compatible Zig 0.16 rewrite: subsystem boundaries, compatibility risks, sequencing, and a realistic effort range.
- [x] Complete the 13-dimension deep review in `CODE_REVIEW.md`, repair the aggregate test runner and coprocess pipe-exhaustion paths with regressions, and make every upstream driver pass in a hermetic Nix environment. (2026-07-24 03:06 EDT) Curiosity poke: a green check must never suppress a driver failure or borrow undeclared host state.
- [x] Rename the top-level README to `README.md` so GitHub renders the canonical Mechatron Prime CI badge, and update the install manifest to ship the renamed file. (2026-07-24 13:07 EDT; hermetic package and full test targets pass.)
- [x] Add a pinned Zig 0.16 C-compiler variant and prove its hermetic package and complete upstream test targets. (2026-07-24 14:08 EDT; local `./test --zig` and Mechatron `b7572575` pass.) Curiosity poke: distinguish compiler diagnostics from Bash compatibility regressions.
- [x] Establish a repeatable GCC-versus-Zig release benchmark with wall-clock and CPU-time history. (2026-07-24 14:08 EDT; first ten-run GCC/Zig baseline recorded.) Curiosity poke: avoid mistaking process startup, debug code, or host scheduling noise for evaluator performance.
- [ ] Prototype a sudo-created Linux COW mutation envelope before adding Bash-level policy hooks. Curiosity poke: Bash can gate resolved redirections, but external descendants and non-redirection writes require an OS-level rollback boundary.
- [ ] Eliminate remaining test timing/global-fixture risks and assess the dynamic-builtin lifecycle leak in separate red–green units.
- [x] Resolve the red Mechatron badge at `dd729522`. Root cause was **not** a Zig defect: the `checks.x86_64-linux.zig-test` target never executed — the builder for `bash-5.3-p15` was killed (truncated log, exit 2, no diagnostic, ~7 min against a 7200 s timeout). Mechatron rebuilt the identical derivation path on the same host to exit 0, exonerating our source by construction. (2026-07-28 17:45 EDT) Curiosity poke: a killed builder is currently recorded identically to a genuine test failure — treat a red badge with no driver name as unexplained until the worker log names one.
- [ ] Note for future CI reads: Mechatron dedupes `(repo, sha)` against a permanent append-only ledger, so a commit that already produced a result can **never** be retried. Clearing a stale red badge requires a new commit, not a re-push or webhook redelivery.
- [ ] Decide whether `rash --emit-ast` earns an implementation session against the Mecha Validate / Rotshield revenue deadline. Design pushback delivered to Einstein 2026-07-28. Established from source: emit at the `eval.c:147-154` `dispose_command` site; `--dump-po-strings` (`shell.c:509-510`) is existing precedent for parse-and-emit-don't-run; `print_cmd.c:176` is the complete visitor to mirror. Constraints corrected: `$(...)` is text in `WORD_DESC`, not a node (`parse.y:4451`); no column positions exist anywhere (`command.h:131-134`); `--emit-ast` must imply `--norc --noprofile` or startup files execute before parsing. Curiosity poke: in-process policy hooks are a footgun guard, never a security boundary — the adversary owns the process.
- [~] Present as `bash` when invoked through a symlink or filename of that name. **Mechanism landed 2026-08-04 21:30 EDT, applied to `--about` only.** `shell_identity ()` in `shell.c` decides the name from argv[0] via the existing `base_pathname` path (so `exec -a` is honored like a symlink, and a login shell's leading `-` is stripped): `bash`, `sh`, and `rbash` answer as bash, anything else as rash. Tested as a classifier over the whole set of invocation names, not just the happy case — `tests/identity.*`. Deliberately **not** yet applied to `--help` or `--version`: those are existing surfaces that scripts parse, there is no rash-specific text in them to hide yet, and changing them is part of the rename rather than of the escape hatch. `--about` is a new option, so it cannot break a caller, and it makes the switch observable and testable now. Remaining: extend to `--help`/`--version` at the moment the binary is actually renamed, and decide then whether `PROGRAM` and the usage banner follow. Prior art is in-tree and should be reused rather than reinvented: `shell.c` already switches on the basename of `$0` for `sh` (`act_like_sh`) and `rbash` (`restricted_shell`). Curiosity poke: this is the compatibility escape hatch that makes the eventual Bash-to-Rash rename safe to perform at all, so it should land *before* any broad rename — and it must be tested as a classifier over the set of invocation names (`bash`, `rash`, `sh`, `rbash`, absolute path, `PATH` lookup, `exec -a`), not just the happy case.
- [ ] Report the `--pretty-print` / ONESHOT defect upstream, from a **pristine** GNU Bash clone (this repo carries Rash, Nix, and CI changes that would contaminate the patch). Defensible framing, which needs no reference to Rash: `ONESHOT` is documented in `config-top.h:34-36` purely as a fork-avoidance efficiency switch, yet it changes observable semantics — with `ONESHOT` defined (the default) `bash --pretty-print -c 'cmd'` executes the command, while a non-`ONESHOT` build prints it and does not. An optimization must not decide whether a command runs. Supporting evidence: `-n` is honored for `-c` (`builtins/evalstring.c:455`), so a do-not-execute mode overriding `-c` is already established in-tree; the interactive ignore-path warns (`shell.c`) while the `-c` ignore-path is silent; and `--pretty-print` appears in `--help` but in no man page. Delivery is a patch to `bug-bash@gnu.org`, **not** a GitHub PR — `bminor/bash` is a read-only mirror and Chet Ramey does not accept PRs there. Keep the patch minimal and separate from the stderr diagnostic, which is a Rash choice and not upstream's problem. Curiosity poke: confirm whether the change stays under the FSF trivial-patch threshold, since anything larger triggers copyright-assignment paperwork.
- [x] Stop `--pretty-print` from executing a `-c` command, and announce the non-execution on stderr for `-c` only. (2026-07-31 18:20 EDT) Root cause was the default-on `ONESHOT` fork-avoidance optimization (`config-top.h:34-36`) bypassing the `pretty_print_mode` check in `shell.c`; the fix routes printing modes through the reader loop that a non-`ONESHOT` build already uses, so no new printing machinery was added. Recorded in `DIVERGENCE.md`, documented in `doc/bash.1` (upstream documented this option nowhere), pinned by `tests/prettyprint.*` across all four invocation routes. Curiosity poke: an efficiency switch silently changing whether a command runs is the exact failure class to sweep the rest of the tree for.
- [ ] Investigate lost SIGCHLD trap invocations in upstream GNU Bash. `tests/trap8.sub` expects 4 `CHLD` firings and intermittently produces 3 (occasionally 2). Measured 2026-07-31: our Nix/GCC build 8/120 failures at 8-way parallelism (6.7%) and 3/60 serial (5.0%); **stock system bash 5.3.15, unmodified and outside Nix, 8/120 (6.7%)** — so it is upstream, not Rash, Nix, or a GCC-versus-Zig difference, and load only nudges the rate rather than causing it. This is not an inherently racy expectation: `jobs.c:4267` passes `children_exited` into `run_sigchld_trap`, which loops `for (i = 0; i < nchild; i++)` at `jobs.c:4527` specifically so that kernel signal coalescing cannot lose firings. Design intent is an exact count, so a lost firing is a defect. Suspect the two delivery routes — the direct call at `jobs.c:4267` versus the deferred `run_pending_traps` path noted at `jobs.c:4247` — racing over the counter. Curiosity poke: do NOT weaken or skip the test; it is correctly detecting a real bug, and suppressing it would convert a true red into a silent one. Note the operational consequence: at ~5-7% per run across two CI targets, roughly one push in eight will show an unexplained red badge.
- [~] SIGCHLD lost-trap-firing race: **reproduced, isolated, documented, NOT fixed.** (2026-07-31 19:40 EDT) Reproduction and full isolation matrix in `BUGFIX_REPRO_REPORTS/2026-07-31-sigchld-lost-trap-firings.md`. Established: reaping a **foreground child** is the trigger (background-only reaping takes the `wait_builtin` branch and never touches `pending_traps` — 20/20 clean; a foreground *busy loop* is also 25/25 clean, so leaving the `wait` builtin is not sufficient); job control is not required; the rate scales with children reaped in the window. Ruled out: the `run_pending_traps` read-then-clear window — blocking SIGCHLD across it changed 9/30 to 7/30, i.e. nothing. The reproduction deliberately does NOT live in `tests/`, because a permanently red suite is worse than the intermittent failure it documents; promote it to `tests/trapchld.*` once fixed. Curiosity poke: next step is an instrumented build logging `children_exited` and `pending_traps[SIGCHLD]` at each `jobs.c` dispatch branch — reasoning has been exhausted, only measurement will pin the statement.
- [x] Implement `--emit-ast` v1: JSON parse-tree emission that never expands and never executes. (2026-07-31 19:55 EDT) Emitter lives in `print_cmd.c` beside the existing text printer and `emit_ast_loop` in `eval.c` beside `pretty_print_loop`, so no build-system changes were needed. Documented in `doc/bash.1` and `DIVERGENCE.md`. Curiosity poke: the first build emitted `"line":-538976289` (0xDFDFDFDF) for `for` loops because `COMMAND.line` is only populated for some node types — caught by looking at real output, not by the property assertions, which all passed.
- [ ] Extend `--emit-ast` beyond `simple` and `connection`: recurse into `for`, `if`, `group`, `subshell`, `case`, `function_def`, and `coproc`, and map `W_*` flag bits to names. Curiosity poke: emit each type's own line number, which lives on the per-type struct rather than on `COMMAND`.
- [ ] Build the LuaJIT command-lifecycle hook layer per `HOOKS_DESIGN.md`: Rack-style `hook(cmd, run)`, FFI userdata proxies rather than tree marshalling, `rash.spawn()` as an unforgeable bypass path instead of an inheritable env marker, `__newindex` write journaling instead of tree diffing, root-owned enforcing hooks versus user-owned advisory ones, and post-parse (unexpanded) as the default stage so variable values cannot leak into hook logs.
- [x] **Fix the SIGCHLD lost-trap-firing race.** (2026-07-31 20:25 EDT) Root cause: `run_pending_traps` ran the queued firings once, then `continue`d to the next signal, so children reaped *during* trap execution queued firings nothing revisited in that pass — and at end of input nothing parsed again to collect them. Found by instrumentation after reasoning failed twice: residual `pending_traps[SIGCHLD]` at exit exactly equalled the missing firings in every run. Fix is `if` to bounded `while`, 9 lines. Repro 60/60, `trap8.sub` 120/120 at 8-way load (was 8/120 failing), both suites green. Curiosity poke: every theory formed by reading the code was wrong; the counters were right in one run. Reach for measurement sooner.
- [x] **Fix the second SIGCHLD loss mechanism (`be829983` was incomplete).** (2026-08-04 19:05 EDT) Codex was right. Reproduced it: `waitchld` runs the trap directly for the `wait` builtin without setting `SIG_INPROGRESS`, and its dispatch chain never checks whether firings are already queued, so a recursive `run_pending_traps` from inside that trap matches no SIGCHLD branch and the generic bad-handler branch warns and clears the backlog. Captured with counters: `b4=1, ran=10 of 11` on failing rounds, `b4=0` on every clean one, with `pending_traps[SIGCHLD]` already non-zero at the direct call and nothing queued during it. Fix is two parts — recognize a running trap by the `IMPOSSIBLE_TRAP_HANDLER` sentinel alone, then drain the deferred firings at the direct dispatch site. The first part alone measured `b4=0` but `pending_at_exit=1` and `ran=10`: it converted a loud loss into a silent one, which is why both are needed. Job control off is what steers `waitchld` to the direct call; `set -m` hides it entirely. Stock 5.3.15 loses 11 of 60 on the new workload, fixed build 0 of 60; both suites green; bounded and unbounded self-spawning traps terminate exactly as stock does. Report: `BUGFIX_REPRO_REPORTS/2026-08-04-sigchld-discarded-on-direct-dispatch.md`; ledger entry 4 in `DIVERGENCE.md`. Upgraded 2026-08-04 20:45 EDT after Grok's independent review (`GROK_REVIEW_AND_MITIGATIONS.md`) confirmed the mechanism and, more usefully, supplied a far better reproduction than mine: a finite self-spawning trap with instant-exit `(exit 0) &` children instead of `sleep`, which puts reaps *inside* the trap window. Measured over 2000 serial rounds — stock 5.3.15 loses 766, this tree with only `be829983` loses 14 (all warning), with both fixes 0. That turned the regression test from a canary into a real gate (1000 rounds, ~19s, ~7 expected failures on regression). Grok saw the rate hold up under parallel load; on this machine parallelism suppresses it entirely (0/800 unfixed), so the gate runs serially. Curiosity pokes: (1) I twice called a null result evidence of absence — the 180 clean rounds that "refuted" Codex, and the load/clear window I "ruled out" at 9/30 to 7/30. Against a sub-1% event neither run had the power to conclude anything. (2) The natural rate moves with machine load by more than an order of magnitude: the same unmodified binary lost firings twice in 400 serial rounds, then ran 8600 clean. Sizing a stress test from a rate measured once is not sound. (3) My first stress test was vacuous — it used `set -m`, which suppresses the very path under test, and passed on unfixed code. Validate red on unfixed code before trusting any statistical gate.
- [x] **Fix the third SIGCHLD loss mechanism (lost update in the drain's read-then-clear).** (2026-08-05 ~10:30 EDT) The armchair suspicion from the very first day was right and the "ruling out" was wrong. `pending_traps[SIGCHLD]` is incremented from handler context (`sigchld_handler` → `waitchld` → `queue_sigchld_trap`) while the drains do a plain `x = pending; pending = 0` — a handler landing between the read and the clear has its increment zeroed uncaptured. Diagnosed by detector: window flags around both the queue-side `+=` and the drain-side read/clear, with a hit counter incremented when `queue_sigchld_trap` runs nested inside a flagged window (single-threaded shell → nested caller can only be the handler). 8000 loaded rounds: 15 losses, 14 with a drain-window hit, write-side hits zero (already covered by `queue_sigchld` deferral + `wait_for` BLOCK_CHILD). Fix: BLOCK_CHILD across the two-line read/clear at both drain sites, released before the trap body runs. Intervention verified by the detector itself: same campaign, 0 losses, 0 hits (p≈e⁻¹⁵). Count assertion restored to `tests/trapchld.tests`. Report: `BUGFIX_REPRO_REPORTS/2026-08-05-sigchld-lost-update-drain-window.md`; DIVERGENCE entry 5. Curiosity pokes: (1) the day-one BLOCK_CHILD test was aimed at the right window and read through the wrong noise floor — when a null result arrives while a 20×-louder bug is live, it measures the loud bug, not the quiet one. (2) The write side was left untouched on measurement, not symmetry; guarding it "for safety" would have been untestable cargo cult. (3) Residual no-DBG failures (~1/8000) may be a fourth thing or a harness artifact; the three-channel campaign (stdout + O_APPEND file + separated stderr) decides.
- [ ] **Upstream bug found, deliberately NOT fixed here: the SIGCHLD path allocates from signal-handler context.** (2026-08-05 ~10:35 EDT) `sigchld_handler` calls `waitchld` directly, and `waitchld` reaches `xmalloc` (via `set_pipestatus_array`, `bgp_add`, and `run_sigchld_trap` → `unwind_protect_mem`). `malloc` is not async-signal-safe, so a SIGCHLD landing while main context is inside malloc/free corrupts the heap. Confirmed on **stock 5.3.15** (2/12000 under load: SIGABRT from glibc `malloc_printerr`, backtrace bottoming out in `xmalloc` ← `unwind_protect_mem` ← `run_sigchld_trap` ← `waitchld`) and on our build (1/24000: SIGSEGV reading `0xdfdfdfdfdfdfdfdf`, bash's own free-fill from `lib/malloc/malloc.c:894`, faulting at `wait.def:271` on a `wait` invoked with no arguments). Two allocators, two signatures, one cause. Reachable from a plain script — SIGCHLD trap plus background children, no `set -m`, no exotic builtins. NOT fixed because the fix is architectural (do the async-signal-safe minimum in handler context, defer all allocation to the next synchronous drain) and that is Chet's call, not a patch to guess at. Report: `BUGFIX_REPRO_REPORTS/2026-08-05-sigchld-async-signal-unsafe-allocation.md`. Curiosity pokes: (1) this is why rounds lost a firing with *no* DBG output — the shell died before its exit dump, and `rc=139`/`rc=134` said so the moment I bothered to capture exit status. Counting output lines while ignoring exit status hid a crash as a "lost firing" for two full campaigns. (2) The rate rises with load, so it is worst exactly when a machine is busy.
- [x] **Chet-ready patch series built and verified.** (2026-08-05 10:50 EDT) `BUGFIX_REPRO_REPORTS/sigchld-fixes.patch` carries all three fixes plus tests against `075aa54e` (a tree with none of them), applies cleanly to a pristine checkout, and takes `tests/trapchld` from 9-of-30 / 6-of-1000 / 416-of-1000 to zero with the full upstream suite green. Regenerate after any further SIGCHLD commit: keep `BUGFIX_REPRO_REPORTS/sigchld-fixes.patch` regenerated after each SIGCHLD commit — code files only (`trap.c`, `jobs.c`, `trap.h`), tests included as their own hunk, no Rash-specific files — so the upstream mail is always one attachment away. The three reports are the mail body; Peter sends personally, no AI attribution. (found 2026-08-04 21:10 EDT) Distinct signature from either fixed class: `waitchld` accounts a reap that is then **neither queued nor run directly** — no `bad value in trap_list` warning, nothing left pending at exit. Captured with counters on Grok's scenario E probe: failing rounds read `reaped=10 queued=9 ran=9 defer=5 b4=0 drain=0 pending_at_exit=0` against clean rounds at `reaped=10 queued=8 ran=10`. Rate is roughly 1 round in 1000 **under CPU load** and near zero unloaded (8/4000, then 1/4000, then 1/6000 — the variance is the story). Ruled out already: the `waitchld` dispatch guard, via a counter on the exact `if (children_exited && ...)` condition, which read `dropped=0` on the failing round, so the reap enters the dispatch block and vanishes inside it. This is the shape Codex originally reported (`reaped=11 queued=10`) and that Grok could not reproduce in 500+ rounds; scenario E under load is the way in. Because it is unfixed, the firing-count assertion is deliberately **not** in `tests/trapchld.tests` — an intermittently red suite is worse than the bug it documents, the same call recorded for the first mechanism. The suite asserts the discarding branch is never taken, which is stable. Curiosity poke: the next counter to add is one per branch of the `waitchld` dispatch chain, since the reap provably enters the block and no branch appears to consume it; suspect the posix `wait_builtin` arm with its `sh_longjmp`, which can leave the block without queueing.
- [ ] **Send both SIGCHLD reports upstream** to `bug-bash@gnu.org` from a pristine clone, as one message — entries 3 and 4 fix two halves of the same contract and a partial fix is worse than none. Peter sends personally; no AI attribution.
- [ ] ~~`be829983` is incomplete.~~ (superseded by the entry above) Codex's independent verification found a second SIGCHLD loss mechanism my `while` fix does not close. Its evidence: counters showing `reaped=11 queued=10 started=10 finished=10 pending=0` (so the firing is lost at or before queueing, not left residual), plus two failing rounds (10-of-11 at round 61/120 no-job-control; 6-of-7 self-spawning at round 24/60). I could NOT reproduce either in 180 rounds, so its rate is far below the one I fixed, but I confirmed the mechanism structurally: `run_sigchld_trap` (`jobs.c`) sets `running_trap` and calls `set_impossible_sigchld_trap()` but never sets `SIG_INPROGRESS`; the `running_trap > 0` guard at `trap.c:347` returns early only for SIGWINCH, so a recursive `run_pending_traps` during a directly-invoked trap skips the three SIGCHLD branches and lands in the generic bad-handler path. Upstream flagged this exact gap at `trap.c:354` ("could check for running the trap handler for the same signal here"). Codex also reports its earlier BLOCK_CHILD guard around the load/clear fixed one no-job-control case, meaning my original "ruled out" verdict on that window was wrong too. Next: set `SIG_INPROGRESS` around the direct `run_sigchld_trap` call, re-test, and only then update the upstream report. Curiosity poke: do not send the bug-bash patch until this is settled; a partial fix mailed upstream is worse than none.
